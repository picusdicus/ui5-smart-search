@baseUrl = http://localhost:4004

### Analytical - revenue by product category (→ SQL path)
POST {{baseUrl}}/search/searchAI
Content-Type: application/json

{ "query": "monthly revenue by product category" }

###

### Analytical - best selling products (→ SQL path)
POST {{baseUrl}}/search/searchAI
Content-Type: application/json

{ "query": "top 5 best selling products" }

###

### Analytical - overdue invoices per country (→ SQL path)
POST {{baseUrl}}/search/searchAI
Content-Type: application/json

{ "query": "how many overdue invoices per country" }

###

### Analytical - top customers by purchase amount (→ SQL path)
POST {{baseUrl}}/search/searchAI
Content-Type: application/json

{ "query": "top customers by total purchase amount" }

###

### Lookup - overdue invoices from Germany (→ RAG path)
POST {{baseUrl}}/search/searchAI
Content-Type: application/json

{ "query": "show me overdue invoices from Germany" }

###

### Lookup - customers from Mexico (→ RAG path)
POST {{baseUrl}}/search/searchAI
Content-Type: application/json

{ "query": "customers from Mexico" }
