
Use ai-agent for this task. The cross-entity search is working but the vector search results 
don't match the actual overdue invoices in HANA. 
The embeddings need richer text to improve semantic accuracy.

─────────────────────────────────────────────────────────
1. UPDATE srv/lib/seed-embeddings.js
─────────────────────────────────────────────────────────
Improve the text used to generate embeddings per entity.
Richer text = better semantic search results.

Customers:
  `${name} customer from ${country}. Email: ${email}. 
   Address: ${address}`

Products:
  `${name}. Category: ${category}. 
   Description: ${description}. 
   Price: ${price} ${currency}. Stock: ${stock} units`

SalesOrders:
  `Sales order status ${status} dated ${orderDate}. 
   Total amount: ${totalAmount} ${currency}. 
   Notes: ${notes}`

Invoices:
  `${status} invoice of ${amount} ${currency}. 
   Due date: ${dueDate}. Invoice date: ${invoiceDate}. 
   Notes: ${notes}`

After updating, re-run the seed script to regenerate 
ALL embeddings with the richer text.

─────────────────────────────────────────────────────────
2. UPDATE srv/lib/context-builder.js — buildContextBlock()
─────────────────────────────────────────────────────────
Include more fields in the context block so Llama 
has full information:

For Invoices:
  `- ID: ${ID} | ${status} | ${amount} ${currency} | 
    Due: ${dueDate} | Customer: ${customerName} 
    (${customerCountry})`

For SalesOrders:
  `- ID: ${ID} | ${status} | ${totalAmount} ${currency} | 
    Date: ${orderDate} | Customer: ${customerName} 
    (${customerCountry})`

For Customers:
  `- ${name} | ${country} | ${email}`

For Products:
  `- ${name} | ${category} | ${price} ${currency} | 
    Stock: ${stock}`