
Use ai-agent for this task. The cross-entity search is working but has 3 issues to fix.
Do NOT rewrite existing logic, only fix the specific problems below.

─────────────────────────────────────────────────────────
FIX 1: context-builder.js — enrichWithRelationships()
─────────────────────────────────────────────────────────
The customer name is not reaching the context block.

Current chain that is failing:
  Invoice → salesOrder_ID → SalesOrder → customer_ID → Customer

Fix enrichWithRelationships() to:
- For each Invoice row: 
    1. Query SalesOrders where ID = invoice.SALESORDER_ID
    2. From that SalesOrder, get CUSTOMER_ID
    3. Query Customers where ID = customer_ID
    4. Add customer.NAME to invoice.relatedData.customerName
- For each SalesOrder row:
    1. Query Customers where ID = salesOrder.CUSTOMER_ID  
    2. Add customer.NAME to salesOrder.relatedData.customerName
- All queries must use Promise.all for parallelism
- If any lookup fails, set customerName = 'Unknown'

─────────────────────────────────────────────────────────
FIX 2: context-builder.js — mergeResults()
─────────────────────────────────────────────────────────
Same invoice ID appearing under multiple entity results.

Fix mergeResults() to:
- Deduplicate using composite key: entityType + ID
  key = `${row.entityType}-${row.ID}`
- Use a Map to track seen keys
- Keep the row with the HIGHEST score if duplicate found
- Never add the same entityType+ID combination twice

─────────────────────────────────────────────────────────
FIX 3: ollama-client.js — askLlamaMultiEntity()
─────────────────────────────────────────────────────────
Llama is adding totals across different currencies.

Update the system prompt to add these rules:
- "NEVER add amounts from different currencies together"
- "Group totals BY currency when multiple currencies exist"
- "Always show the currency code next to every amount"
- "If customer name is available in relatedData, 
   always use it instead of showing the raw ID"

─────────────────────────────────────────────────────────
After all fixes, also update buildContextBlock() to:
- Include relatedData.customerName in the formatted 
  output if present:
  "- ID: xxx | Amount: EUR 5,000 | Status: Overdue | 
     Customer: Acme GmbH"
  instead of just showing the raw ID
─────────────────────────────────────────────────────────